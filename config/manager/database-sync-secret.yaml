# NOTE: This file is NOT NEEDED when using the existing PostgreSQL secret
# The operator is configured to use partnerlabs-datastore-pguser-labrat secret
# in the partnerlabs-wg-cluster namespace.
#
# This file is kept as a reference template if you need to configure
# a different database or deploy to a different environment.
#
# If you want to use this secret instead of the existing one:
# 1. Uncomment the resource below
# 2. Update the values with your database credentials
# 3. Add this file back to config/manager/kustomization.yaml resources
# 4. Update config/manager/manager.yaml to reference this secret instead

# apiVersion: v1
# kind: Secret
# metadata:
#   name: database-sync-secret
#   namespace: system
# type: Opaque
# stringData:
#   # Database credentials
#   DB_USERNAME: "opl_operator"
#   DB_PASSWORD: "changeme"
