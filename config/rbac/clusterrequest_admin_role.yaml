# This rule is not used by the project opl-cluster-operator itself.
# It is provided to allow the cluster admin to help manage permissions for users.
#
# Grants full permissions ('*') over opl.openshiftpartnerlabs.com.
# This role is intended for users authorized to modify roles and bindings within the cluster,
# enabling them to delegate specific permissions to other users or groups as needed.

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: opl-cluster-operator
    app.kubernetes.io/managed-by: kustomize
  name: clusterrequest-admin-role
rules:
- apiGroups:
  - opl.openshiftpartnerlabs.com
  resources:
  - clusterrequests
  verbs:
  - '*'
- apiGroups:
  - opl.openshiftpartnerlabs.com
  resources:
  - clusterrequests/status
  verbs:
  - get
