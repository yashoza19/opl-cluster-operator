apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  creationTimestamp: null
  name: clusterrequests.opl.openshiftpartnerlabs.com
spec:
  group: opl.openshiftpartnerlabs.com
  names:
    kind: ClusterRequest
    listKind: ClusterRequestList
    plural: clusterrequests
    shortNames:
    - cr
    - creq
    singular: clusterrequest
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.clusterName
      name: Cluster
      type: string
    - jsonPath: .spec.environment
      name: Environment
      type: string
    - jsonPath: .spec.clusterSize
      name: Size
      type: string
    - jsonPath: .status.state
      name: State
      type: string
    - jsonPath: .status.gitCommitSHA
      name: GitCommit
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ClusterRequest is the Schema for the clusterrequests API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ClusterRequestSpec defines the desired state of ClusterRequest.
            properties:
              baseDomain:
                default: openshiftpartnerlabs.com
                description: BaseDomain is the base DNS domain for the cluster
                type: string
              clusterName:
                description: ClusterName is the name of the OpenShift cluster to provision
                maxLength: 63
                minLength: 1
                pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                type: string
              clusterSize:
                description: ClusterSize defines the cluster configuration size (small,
                  medium, large)
                enum:
                - small
                - medium
                - large
                type: string
              companyName:
                description: CompanyName is the company/organization name
                type: string
              controlPlane:
                description: ControlPlane defines control plane node configuration
                properties:
                  instanceType:
                    default: m5.xlarge
                    description: InstanceType is the AWS instance type for control
                      plane nodes
                    type: string
                  replicas:
                    default: 3
                    description: Replicas is the number of control plane nodes
                    maximum: 5
                    minimum: 1
                    type: integer
                required:
                - replicas
                type: object
              environment:
                description: Environment specifies the environment type (development,
                  staging, production)
                enum:
                - development
                - staging
                - production
                type: string
              labId:
                description: LabID is the reference to the database lab ID
                type: integer
              networking:
                description: Networking defines cluster networking configuration
                properties:
                  clusterNetworkCIDR:
                    default: 10.128.0.0/14
                    description: ClusterNetworkCIDR is the CIDR for pod networking
                    type: string
                  machineNetworkCIDR:
                    default: 10.0.0.0/16
                    description: MachineNetworkCIDR is the CIDR for machine networking
                    type: string
                  serviceNetworkCIDR:
                    default: 172.30.0.0/16
                    description: ServiceNetworkCIDR is the CIDR for service networking
                    type: string
                type: object
              openshiftVersion:
                default: img4.20.10-x86-64-appsub
                description: OpenshiftVersion specifies the OpenShift version image
                  reference
                type: string
              region:
                description: Region specifies the AWS region for cluster deployment
                type: string
              requestedBy:
                description: RequestedBy is the username/email of the person requesting
                  the cluster
                type: string
              workers:
                description: Workers defines worker node configuration
                properties:
                  instanceType:
                    default: m5.2xlarge
                    description: InstanceType is the AWS instance type for worker
                      nodes
                    type: string
                  replicas:
                    default: 3
                    description: Replicas is the number of worker nodes
                    maximum: 100
                    minimum: 0
                    type: integer
                  zones:
                    description: Zones is the list of availability zones for worker
                      nodes
                    items:
                      type: string
                    type: array
                required:
                - replicas
                type: object
            required:
            - baseDomain
            - clusterName
            - clusterSize
            - environment
            - openshiftVersion
            - region
            - requestedBy
            type: object
          status:
            description: ClusterRequestStatus defines the observed state of ClusterRequest.
            properties:
              argocdAppCreated:
                description: ArgocdAppCreated indicates if the ArgoCD Application
                  was created
                type: boolean
              clusterProvisioned:
                description: ClusterProvisioned indicates if the cluster has been
                  successfully provisioned
                type: boolean
              conditions:
                description: Conditions represent the latest available observations
                  of the cluster request's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              errorMessage:
                description: ErrorMessage contains error details if the request failed
                type: string
              gitCommitSHA:
                description: GitCommitSHA is the SHA of the git commit that created
                  the cluster configuration
                type: string
              hiveClusterID:
                description: HiveClusterID is the Hive cluster ID from ClusterDeployment
                type: string
              hiveInfraID:
                description: HiveInfraID is the infrastructure ID from Hive
                type: string
              processingCompletedAt:
                description: ProcessingCompletedAt is when processing completed
                format: date-time
                type: string
              processingStartedAt:
                description: ProcessingStartedAt is when processing began
                format: date-time
                type: string
              retryCount:
                description: RetryCount tracks the number of reconciliation retries
                type: integer
              state:
                description: State represents the current state of the cluster request
                enum:
                - pending
                - approved
                - git-committed
                - provisioning
                - complete
                - failed
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: null
  storedVersions: null
